{
  "compilerOptions": {
    "target": "ESNext",
    "module": "Preserve",
    "lib": [
      "esnext",
      "DOM",
      "DOM.Iterable",
    ],
    "noEmit": true,
    "strict": true,
    "sourceMap": true,
    "allowImportingTsExtensions": true,
    "rewriteRelativeImportExtensions": true,
    "esModuleInterop": true,// import fs from 'fs'; が使えるようになる
    "verbatimModuleSyntax": true, // Typescript 5.0で追加。typeをimportした時、import文をimport {type xxx} from "xxx"と書かないとエラーになる。
    "erasableSyntaxOnly": true, // Typescript 5.8で追加。enumやnamespaceが完全に禁止になる
    "types": [
      // "vitest/globals", // .test.ts の中で直接itやexceptを使う時はコメントを解除する
      "vitest/importMeta", // In-Source テストを行う時はコメントを解除する
    ]
  },
  "ts-node": {
    "files": true
  }
}